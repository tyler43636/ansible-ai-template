---
all:
  hosts:
    localhost:
      ansible_connection: local
  children:
    # Add your host groups here
    # Example:
    # webservers:
    #   hosts:
    #     web01.example.com:
    #     web02.example.com:
